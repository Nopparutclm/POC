import { CSSProperties, FC, ReactNode } from "react";
import { Point2D } from "./hooks/usePan";
interface Props {
    children: ReactNode;
    className?: string;
    style?: CSSProperties;
    defaultValue?: Point2D;
    value?: Point2D;
    onChange?(delta: Point2D): any;
}
export declare const Pan: FC<Props>;
export default Pan;
