import { FC, ReactNode } from "react";
import { SketchConfig, IFrameConfigBase } from "../../config";
interface ParseSucceeded<T> {
    valid: true;
    data: T;
}
interface ParseFailed {
    valid: false;
    error: ReactNode;
}
type Parser<T> = (url: URL) => ParseSucceeded<T> | ParseFailed;
export declare const v1UrlParser: Parser<IFrameConfigBase>;
interface Props {
    config: SketchConfig;
}
export declare const Sketch: FC<Props>;
export {};
